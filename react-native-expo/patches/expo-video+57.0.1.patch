diff --git a/node_modules/expo-video/build/VideoPlayer.web.js b/node_modules/expo-video/build/VideoPlayer.web.js
index 46dfa83..3707636 100644
--- a/node_modules/expo-video/build/VideoPlayer.web.js
+++ b/node_modules/expo-video/build/VideoPlayer.web.js
@@ -1,5 +1,11 @@
 import { useMemo } from 'react';
 import resolveAssetSource from './resolveAssetSource';
+function safePlay(video) {
+    const result = video.play();
+    if (result && typeof result.catch === 'function') {
+        result.catch(() => undefined);
+    }
+}
 export function useVideoPlayer(source, setup) {
     const parsedSource = typeof source === 'string' ? { uri: source } : source;
     return useMemo(() => {
@@ -228,7 +234,7 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
     }
     play() {
         this._mountedVideos.forEach((video) => {
-            video.play();
+            safePlay(video);
         });
     }
     pause() {
@@ -237,13 +243,18 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
         });
     }
     replace(source) {
+        const uri = getSourceUri(source);
         this._mountedVideos.forEach((video) => {
-            const uri = getSourceUri(source);
             video.pause();
             if (uri) {
                 video.setAttribute('src', uri);
+                const onCanPlay = () => {
+                    if (getSourceUri(this.src) === uri && video.getAttribute('src') === uri) {
+                        safePlay(video);
+                    }
+                };
+                video.addEventListener('canplay', onCanPlay, { once: true });
                 video.load();
-                video.play();
             }
             else {
                 video.removeAttribute('src');
@@ -268,7 +279,7 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
     replay() {
         this._mountedVideos.forEach((video) => {
             video.currentTime = 0;
-            video.play();
+            safePlay(video);
         });
         this.playing = true;
     }
@@ -283,7 +294,7 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
             video.pause();
         }
         else {
-            video.play();
+            safePlay(video);
         }
         video.currentTime = firstVideo.currentTime;
         video.volume = firstVideo.volume;
@@ -309,7 +320,7 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
             this.playing = true;
             this._mountedVideos.forEach((mountedVideo) => {
                 if (e.target !== mountedVideo) {
-                    mountedVideo.play();
+                    safePlay(mountedVideo);
                 }
             });
         };
diff --git a/node_modules/expo-video/src/VideoPlayer.web.tsx b/node_modules/expo-video/src/VideoPlayer.web.tsx
index 5cfdf59..a829ff4 100644
--- a/node_modules/expo-video/src/VideoPlayer.web.tsx
+++ b/node_modules/expo-video/src/VideoPlayer.web.tsx
@@ -17,6 +17,13 @@ import type { VideoPlayerEvents } from './VideoPlayerEvents.types';
 import type { VideoThumbnail } from './VideoThumbnail';
 import resolveAssetSource from './resolveAssetSource';
 
+function safePlay(video: HTMLVideoElement) {
+  const result = video.play();
+  if (result && typeof result.catch === 'function') {
+    result.catch(() => undefined);
+  }
+}
+
 export function useVideoPlayer(
   source: VideoSource,
   setup?: (player: VideoPlayer) => void
@@ -290,7 +297,7 @@ export default class VideoPlayerWeb
 
   play(): void {
     this._mountedVideos.forEach((video) => {
-      video.play();
+      safePlay(video);
     });
   }
 
@@ -301,13 +308,18 @@ export default class VideoPlayerWeb
   }
 
   replace(source: VideoSource): void {
+    const uri = getSourceUri(source);
     this._mountedVideos.forEach((video) => {
-      const uri = getSourceUri(source);
       video.pause();
       if (uri) {
         video.setAttribute('src', uri);
+        const onCanPlay = () => {
+          if (getSourceUri(this.src) === uri && video.getAttribute('src') === uri) {
+            safePlay(video);
+          }
+        };
+        video.addEventListener('canplay', onCanPlay, { once: true });
         video.load();
-        video.play();
       } else {
         video.removeAttribute('src');
         video.load();
@@ -334,7 +346,7 @@ export default class VideoPlayerWeb
   replay(): void {
     this._mountedVideos.forEach((video) => {
       video.currentTime = 0;
-      video.play();
+      safePlay(video);
     });
     this.playing = true;
   }
@@ -350,7 +362,7 @@ export default class VideoPlayerWeb
     if (firstVideo.paused) {
       video.pause();
     } else {
-      video.play();
+      safePlay(video);
     }
     video.currentTime = firstVideo.currentTime;
     video.volume = firstVideo.volume;
@@ -382,7 +394,7 @@ export default class VideoPlayerWeb
       this.playing = true;
       this._mountedVideos.forEach((mountedVideo) => {
         if (e.target !== mountedVideo) {
-          mountedVideo.play();
+          safePlay(mountedVideo);
         }
       });
     };
