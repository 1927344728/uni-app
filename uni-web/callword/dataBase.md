mysql数据库，创建表和插入数据：
### 第一张表：用户信息
#### 表结构
* id：数值，自增
* uuid：字符串
* 手机号：字符串，11位
* 名称：字符串，256位
* 类型：数值，1-100
* 验证码：字符串，4位
* token：字符串
* 创建时间：时间戳，插入记录时的时间戮
* 更新时间：创建或更新记录的时间
* 删除标识：默认 false
#### 要求
* 输出创建表的 mysql 语句
* 输出插入一条数据的 mysql 语句
* 写入文件，默认0，0-未删除 1-已删除


### 第二张表：用户详细信息
#### 表结构
* id：数值，自增
* 性别：数值，1-男 2-女
* 生日：时间戳
* 学历：数值，1-30，其中1-3对应幼儿园小班、中班、大班，4-12对应小学中学的9个年级，13-15对应高中三年，16及以上对应大学及以上学历
* 父亲id：数值
* 母亲id：数值
* 兄弟姐妹：字符串
* 配偶：数值
* 子女：字符串
* 创建时间：时间戳，插入记录时的时间戮
* 更新时间：创建或更新记录的时间
* 删除标识：默认0，0-未删除 1-已删除
#### 要求
* 输出创建表的 mysql 语句
* 输出插入一条数据的 mysql 语句
* 写入文件，存在 database 目录下

### 第三张表：任务信息
#### 表结构
* id：数值，自增
* 标题：字符串，256位
* 描述：字符串，任意长度
* 图片：字符串，256位
* 接收人：字符串，256位
* 奖励：字符串，256位
* 奖励图片：字符串，256位
* 进度：数值，1-100，允许两位小数
* 开始时间：时间戳
* 结束时间：时间戳
* 结束凭证：字符串，256位
* 结束标识：默认0，0-未结束 1-结束
* 创建时间：时间戳，插入记录时的时间戮
* 更新时间：创建或更新记录的时间
* 删除标识：默认0，0-未删除 1-已删除
#### 要求
* 输出创建表的 mysql 语句
* 输出插入一条数据的 mysql 语句
* 写入文件，存在 database 目录下

请创建一个MySQL书籍信息表的建表语句，要求包含以下字段和特性：
1. 基础字段：
   - 主键ID（自增）
   - 书名（必填）
   - 作者（必填）
   - 书籍语言（支持多语言）
   - ISBN（唯一，13位）
   - 出版社（必填）
   - 出版日期
   - 商品重量（精确到小数点后两位，必须为非负数）
   - 定价（必填，精确到小数点后两位）
   - 库存数量（默认0）
   - 分类ID
   - 封面图片路径：从互联网上，查询免费的＼公开的图片地址
   - 书籍简介
   - 书籍开本（使用ENUM类型，包含：16开、大16开、32开、大32开、64开、其他）
   - 书籍尺寸（记录长×宽×厚，单位：mm）
   - 状态（默认1，1:可售 0:下架）
   - 创建时间（自动记录）
   - 更新时间（自动更新）
2. 插入数据
   - 插入100条数据，包含所有字段
   - 包含多个分类，如：道德经、山海经、国内四大名著等、国外名著、人生文学、历史传记、科普读物、儿童读物等
   - 联网查询，需要真实数据
3. 其他要求：
   - 使用InnoDB引擎
   - 使用utf8mb4字符集
   - 添加适当的索引
   - 所有字段添加中文注释
   - 确保数据完整性约束
   - 输出 mysql 语句到文件，存放在 database 目录下 

请生成完整的SQL建表语句，包含所有字段定义、约束、索引和表选项。


请创建一个MySQL音乐信息表的建表语句，要求包含以下字段和特性：
1. 基础字段
   - id：数值，自增，主键
   - 歌曲名称：字符串，必填
   - 艺术家：字符串，必填
   - 专辑名称：字符串
   - 发行日期：时间戳
   - 流派：字符串（如：流行、摇滚、古典等）
   - 时长：数值，单位秒
   - 语言：字符串
   - 版权信息：字符串
   - 文件格式：字符串（如：MP3、WAV等）
   - 文件大小：数值，单位MB
   - 播放次数：数值，默认0
   - 评分：数值，1-5，允许一位小数
   - 封面图片路径：字符串，图片地址，从互联网上查询公开、免费的图片
   - 歌词：长文本
   - 状态：数值，默认1（1:可用 0:下架）
   - 创建时间：时间戳，自动记录
   - 更新时间：时间戳，自动更新
2. 其他要求
   - 使用InnoDB引擎
   - 使用utf8mb4字符集
   - 添加适当的索引
   - 所有字段添加中文注释
   - 确保数据完整性约束
   - 输出创建表的 mysql 语句
   - 输出50条插入数据的 mysql 语句
   - 插入数据以中文歌曲为主，少量其他语言歌曲
   - 2023年以前的经典歌英和2023-2025年的热闹歌曲
   - 封面图片，从互联网查询公开的、不侵权的图片地址
   - 歌曲不要重复
   - 输出 mysql 语句到文件，存放在 database 目录下